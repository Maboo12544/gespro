import {cookies} from 'next/headers';
import {currentAccess,sessionCookie,authConfig} from '@/lib/gespro-auth';
const headers={'Cache-Control':'no-store'};
export async function GET(request:Request){try{const access=await currentAccess();if(!access)return Response.json({error:'Konekte ankò.'},{status:401,headers});const pos=new URL(request.url).searchParams.get('pos');if(!access.points.some(p=>p.id===pos&&p.active))return Response.json({error:'POS pa disponib.'},{status:403,headers});const token=(await cookies()).get(sessionCookie)?.value;const {url,key}=authConfig();const r=await fetch(`${url}/rest/v1/rpc/gespro_pos_configuration`,{method:'POST',headers:{apikey:key,Authorization:`Bearer ${token}`,'Content-Type':'application/json'},body:JSON.stringify({target_pos:pos}),cache:'no-store'});if(!r.ok)return Response.json({error:'Konfigirasyon pa disponib.'},{status:503,headers});return Response.json({configuration:await r.json()},{headers})}catch{return Response.json({error:'Konfigirasyon pa disponib.'},{status:503,headers})}}
